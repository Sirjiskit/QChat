import { Text } from "native-base";
import React, { memo } from "react";
// import MyTab from "../components/navigation/tab.nav";

const HomeScreen = () => {
    return (
        <React.Fragment>
            {/* <MyTab/> */}
        </React.Fragment>
    )
}
export default memo(HomeScreen);